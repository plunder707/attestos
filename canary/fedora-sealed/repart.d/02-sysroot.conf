# The upstream definition uses Encrypt=key-file. This disposable, no-network
# positive control deliberately omits encryption because root confidentiality
# is outside the UKI/PCR admission claim and interactive unlock is timing-bound.
[Partition]
Type=root
Format=btrfs
SizeMinBytes=10G
