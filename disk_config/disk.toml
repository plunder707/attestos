# This pinned bootc-image-builder rejects external filesystem customizations
# for qcow2. The candidate image carries its bounded partition contract at the
# canonical /usr/lib/image-builder/bootc/disk.yaml path instead.
